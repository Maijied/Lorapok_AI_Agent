{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "."
      ],
      "description": "Provides secure workspace filesystem read, write, and directory inspection capabilities."
    },
    "git": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-server-git",
        "--repository",
        "."
      ],
      "description": "Provides git branch management, commit generation, and diff inspection capabilities."
    },
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "description": "Manage GitHub issues, PRs, and repositories. Relies on GITHUB_PERSONAL_ACCESS_TOKEN from environment."
    },
    "memory": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-memory"
      ],
      "description": "Persistent Knowledge Graph memory system for agent context across sessions."
    },
    "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ],
      "description": "Provides a specialized tool for breaking down complex architectural problems."
    },
    "puppeteer": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-puppeteer"
      ],
      "description": "Headless browser automation for testing, scraping docs, and visual inspection."
    },
    "sqlite": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sqlite",
        "--db-path",
        ".lorapok/lancedb/lorapok.db"
      ],
      "description": "Directly inspect and query local .sqlite databases."
    },
    "browsermcp": {
      "command": "npx",
      "args": [
        "@browsermcp/mcp@latest"
      ],
      "description": "Browser MCP for checking web content like GitHub Actions."
    }
  }
}
