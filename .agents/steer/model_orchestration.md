# Model Orchestration Steer

- Single catalog pipeline in ModelManager
- Usable vs Paid invariants (Google free-API = usable)
- Fallback via `pickFallbackModelId`; persist model only after success
- CLI menus and REST share the same views
- Registry parity for `/model` and `/refresh-models`
